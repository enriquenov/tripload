import React from 'react'
// import PropTypes from 'prop-types'
// import {connect} from 'react-redux'

/**
 * COMPONENT
 */

export class YourTripsView extends React.Component {
  render() {
    return <div className="container yourtrips" />
  }
}

/**
 * CONTAINER
 */
// const mapState = state => ({
//   trips: state.trip
// })

// const mapDispatch = dispatch => ({
//   fetchTrips: id => dispatch(gotTrips(id))
// })

// export default connect(mapState, mapDispatch)(YourTripsView)

/**
 * PROP TYPES
 */
// UserHome.propTypes = {
//   firstName: PropTypes.string
// }
